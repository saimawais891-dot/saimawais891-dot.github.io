LORVÉN — ADMIN PANEL BUILD

Included:
- index.html: current customer storefront
- admin.html: mobile-friendly admin control panel prototype

IMPORTANT:
This admin.html currently stores data in the browser's localStorage. It is NOT a secure production backend and changes are not synchronized across devices.

Production next step:
Connect authentication + database/storage + server-side APIs, then wire the storefront to that database. GitHub Pages can host the frontend but cannot run the secure backend itself.
